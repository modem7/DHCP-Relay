kind: pipeline
type: docker
name: latest

platform:
  os: linux
  arch: amd64

steps:
- name: latest  
  image: thegeeklab/drone-docker-buildx
  privileged: true
  settings:
    repo: modem7/dhcprelay
    purge: true
    platforms: linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm/v8
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    tags: latest

# trigger:
  # event:
  # - cron
  # cron:
  # - dhcprelay_latest

---

kind: pipeline
type: docker
name: stable

platform:
  os: linux
  arch: amd64

steps:
- name: stable  
  image: thegeeklab/drone-docker-buildx
  privileged: true
  settings:
    repo: modem7/dhcprelay
    purge: true
    platforms: linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm/v8
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    tags: stable

# trigger:
  # event:
  # - cron
  # cron:
  # - dhcprelay_stable